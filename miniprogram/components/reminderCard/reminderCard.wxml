<view class="reminder-card {{reminder.status}}">
  <view class="card-left">
    <view class="time-badge">
      <text class="time-text">{{timeText}}</text>
    </view>
  </view>
  
  <view class="card-center">
    <text class="medication-name">{{reminder.medicationName}}</text>
    <text class="dosage-text">{{reminder.dosage}}</text>
  </view>
  
  <view class="card-right">
    <view 
      class="status-btn {{reminder.status}}" 
      wx:if="{{reminder.status === 'pending' || reminder.status === 'sent'}}"
      bind:tap="onTapTake"
    >
      <text>服用</text>
    </view>
    <view class="status-btn completed" wx:elif="{{reminder.status === 'completed'}}">
      <text>已服</text>
    </view>
    <view class="missed-group" wx:elif="{{reminder.status === 'missed'}}">
      <text class="missed-text">漏服</text>
      <view class="missed-actions">
        <view class="late-take-btn" bind:tap="onTapTake">
          <text>漏了但是吃了</text>
        </view>
        <view class="acknowledge-btn" bind:tap="onTapAcknowledge">
          <text>漏服没吃</text>
        </view>
      </view>
    </view>
    <view class="status-btn acknowledged" wx:else>
      <text>已处理</text>
    </view>
  </view>
</view>
